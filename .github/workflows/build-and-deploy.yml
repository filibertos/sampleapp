name: Build and Deploy .NET App

on:
  workflow_dispatch:

jobs:

  build-job:
    runs-on: windows-latest

    steps:

      - name: Checkout code
        uses: actions/checkout@v4.1.7

      - name: Setup .NET core
        uses: actions/setup-dotnet@v4.0.1
        with:
          dotnet-version: '8.x'

      - name: Build code
        run: dotnet build --configuration Release

      - name: Publish code
        run: dotnet publish -c Release --property:PublishDir="${env.DOTNET_ROOT}\myapp"

      - name: upload artifact
        uses: actions/upload-artifact@v4.3.6
        with:
          name: .net-app
          path: ${{ env.DOTNET_ROOT }}\myapp

  #deploy-job:
   # runs-on: windows-latest
   # needs: build-job
   # env:
    #  package-name: .net-app
    #  app-name: azureapp1
     # deployment-slot: production

    #steps:

      #- name: Download artifact
      #  uses: actions/download-artifact@v4.1.8
      #  with:
       #   name: ${{ env.package-name }}

      #- name: Secret information
       # run: echo "Value of secret matches: ${{ secrets.AZUR_PUBLISH_PROFILE == 'Placeholder' }}"

      #- name: Name of app service
      #  run: echo "App will be deployed to ${{ env.app-name }}"

      #- name: List files in root directory
      #  run: |
     #     Get-ChildItem - Path . | ForEach-Object { Write-Output $_.Name }
       # shell: pwshell
            
